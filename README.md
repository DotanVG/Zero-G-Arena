# Orbital Breach

Browser-based zero-gravity FPS scaffold split into `client`, `server`, and `shared` packages.

## Quick Start

```bash
cd client
npm install
npm run dev
```

```bash
cd server
npm install
npm run dev
```
